# Every contributor and CI runner compiles with the same compiler.
# MSRV (rust-version in rust/Cargo.toml) is this version minus two minors.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
